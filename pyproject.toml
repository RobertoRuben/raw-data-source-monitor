[project]
name = "raw-data-source-monitor"
version = "0.1.0"
description = "Your project description"
requires-python = ">=3.13"
dependencies = [
    "httpx[testing]>=0.28.1",
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
    "python-dotenv>=1.0.0",
]

